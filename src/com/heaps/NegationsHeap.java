//Given an array of N Integers, you want to do exactly B negations
//Goal: To Maximize sum of array
//Negations-> multiply with -1
//Idea is to convert into Min Heap, then do deletion of root node B times followed by sum of array elements
package com.heaps;

public class NegationsHeap {
    public static void main(String args[]){
        int arr[]={24,-68,-29,-9,84};
        int n = arr.length;
        //Create Min Heap

    }
}
